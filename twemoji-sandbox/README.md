twemoji	sandbox
---

Probé twemoji, paquete npm de Twitter para convertir strings de emoji a un emoji de Twitter.

El método `parse()` toma como input un string y lo convierte a HTML, con un tag `<img>` por cada
string que corresponde a un emoji.